const numBase = 5
let numLimit = 0
let result

for (numLimit; numLimit <= 10; numLimit++) {
    result = numBase * numLimit
    console.log('El resultado de multiplicar ' + numBase + ' x ' + numLimit + ' es: ' + result)
}